export type Service = {
  title: string;
  description: string;
  href: string;
};

export type Property = {
  title: string;
  location: string;
  price: string;
  type: string;
};

export type Insight = {
  title: string;
  excerpt: string;
  date: string;
};
